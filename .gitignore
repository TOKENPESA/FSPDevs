# Build artifacts
target/
**/target/

# Generated Wasm package (rebuild with wasm-pack)
fiber-agent/pkg/

# Local sidecar runtime state
**/.fiber-agent/

# Dependencies
node_modules/

# Binaries
*.wasm

# Audit / release archives (regenerate locally)
*.zip
*.tgz
FSPDevs-code-audit-v*/
TPXDevs-code-audit-v*/
TPXDevs-gemini-audit-*/
FSPDevs-gemini-audit-*/

# Environment secrets (keep tracked examples)
fsp-console/.env
fsp-console/.env.*
!fsp-console/.env.example

# MFA local supervisor runtime state
master-fiber-agent/.mfa-supervisor/

# Transient tooling output
typecheck-errors.txt

# Upstream Fiber monorepo (not part of FSPDevs workspace)
fiber/

# OS / editor
.DS_Store
Thumbs.db

# Cursor local cache (not source)
.cursor/
