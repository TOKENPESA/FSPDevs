[workspace]
resolver = "2"
members = [
    "mesh-core",
    "master-fiber-agent",
    "fiber-agent",
    "mesh-operator",
    "fsp-fixed-math",
]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
