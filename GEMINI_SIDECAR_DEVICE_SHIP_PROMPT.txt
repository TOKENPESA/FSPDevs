================================================================================
GEMINI PROMPT — Fiber Agent Sidecar Device-Ship Gap Analysis
================================================================================

You are a principal mobile/desktop systems engineer + distributed payments
architect reviewing TOKENPESA / FSPDevs.

Upload and analyze:
  - GEMINI_SIDECAR_DEVICE_SHIP_AUDIT.txt  (current status snapshot — trust as baseline)
  - FSPDevs-gemini-audit-15.zip          (code slice for Sidecar / MFA / FNN / deploy)

PRODUCT INTENT
--------------
We want the Fiber Agent Sidecar (Tauri) to run on a SEPARATE device as:

  A) Windows desktop app
  B) Android app

bundled with (or clearly accompanied by) Fiber Network Node (FNN) so that:
  - a field agent can install the app
  - automatically register to our MFA droplet at mfa.fsprotocol.com
  - use real Fiber *testnet* payments (not silent simulated bookkeeping)
  - operate DiCoBa savings/loans and cash/mobile-money modules

Current deploy:
  MFA:      mfa.fsprotocol.com / 167.99.150.153
  Treasury: 134.122.120.65 (FA-1 + Linux FNN, RPC not public to edge)


YOUR TASK
---------
Produce a blunt, ordered remaining-work plan. Do NOT rewrite history or praise
completed work. Focus on what is LEFT.

### 1) Executive answer (10 lines max)
Can we ship Windows? Android? With bundled FNN or not? What is the minimum
honest product claim we can make this month?

### 2) Gap matrix
Table columns: Gap | Platform (Win/Android/Both) | Severity (P0/P1/P2) |
Blocks register-to-MFA? | Blocks live Fiber payment? | Est. effort

Cover at least:
- FNN packaging / companion install / remote FNN architecture
- Silent simulate fallback while UI says testnet
- MFA auto-register E2E on clean device
- Absolute state dir + installer
- Signing keys / mesh pubkey registry
- Android project init + cleartext/network policy
- JoyID / funding on mobile WebView
- Channel open + liquidity wizard
- DiCoBa leader pubkey + live send_payment path
- Public /api/register abuse controls

### 3) Architecture recommendation (choose ONE primary path)
Recommend the best architecture for "separate device Sidecar + FNN + MFA":

  Option A — Bundle/side-install local FNN on Windows only; Android later as
             remote-control UI to Hub/custody FNN
  Option B — No edge FNN; Sidecar is MFA control UI + Demo modules only;
             Hub FA-1 holds custody
  Option C — Remote FNN RPC gateway (authenticated) for edge devices
  Option D — Other (propose)

Give pros/cons, security implications, and what breaks on Android for each.

### 4) MFA registration — remaining checklist
Exact steps / code / ops still needed for:
  clean install → POST /api/register → persist identity → WSS connected →
  MFA console shows FA-N → signed telemetry accepted.

Call out anything in the zip that looks incomplete or unsafe for public pilot.

### 5) Windows ship checklist (ordered tickets)
Concrete tickets a small team can execute (title + acceptance criteria).
Separate "MFA-only ship" vs "live Fiber payment ship".

### 6) Android ship checklist (ordered tickets)
Including whether on-device FNN is realistic for Fiber/CKB. If not, state
clearly and propose the interim Android product shape.

### 7) Demo vs Live truthfulness
Propose UX/API changes so operators cannot mistake a local DiCoBa UUID receipt
for a Fiber testnet settlement.

### 8) 30 / 60 / 90 day plan
Milestones with exit criteria. No fluff.


CONSTRAINTS / FACTS TO RESPECT
------------------------------
- Do not invent that fnn.exe is already bundled — it is NOT in tauri.conf.json.
- Do not invent Android gen/ project — init scripts exist; APK project may be absent.
- MFA defaults already target TLS hostname mfa.fsprotocol.com.
- Auto-register exists in daemon/Tauri boot.
- Treasury Hub FNN is live but RPC is not meant as public edge wallet RPC.
- DiCoBa "Contribution sent" can be local SQLite-only under simulate/fallback.
- Prefer actionable engineering over marketing language.


OUTPUT FORMAT
-------------
Use clear markdown with numbered sections matching 1–8 above.
Put P0 items first in every list.
End with: "Top 5 things to do this week" as a bullet list.

================================================================================
END PROMPT
================================================================================
